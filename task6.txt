Дана целочисленная матрица А = {aij}, где 1 <= i <= n; 1 <= j <= m; Выбрать строки
матрицы, суммы элементов которых есть простые числа, и отсортировать
выбранные строки по убыванию максимальных элементов таких строк. Если таких
строк в матрице нет, выдать сообщение.
